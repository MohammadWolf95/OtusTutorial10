#ifndef BLOCKOBSERVER_H
#define BLOCKOBSERVER_H

#include "state.h"

class StaticBlock:public Statecout{
public:
    StaticBlock(size_t& N)
        :n_arg(N), count(0){
    }
    void staticblock();
    void dynamicblock();
    void update(const std::string str);
    void init();
    void print_to_file(const std::string &str);
};

#endif // BLOCKOBSERVER_H
